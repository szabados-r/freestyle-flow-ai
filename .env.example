# Copy this file to .env.local (gitignored) and fill in your own values.
# These secrets are NEVER committed — every user must bring their own keys.

# Lovable AI Gateway (Gemini 3 Flash) — bars, scoring, verdicts
# Get one at https://lovable.dev (or use OpenRouter / OpenAI-compatible gateway)
LOVABLE_API_KEY=

# ElevenLabs — Scribe (speech-to-text) + TTS opponent voices
# Get one at https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=